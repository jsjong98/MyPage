# Dependencies — restored with `npm install`, never committed
node_modules/

# Build output — built and deployed by GitHub Actions, not committed
dist/

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Local env files
.env
.env.local
